# AXI4/ACE-Lite异步位宽转换桥测试点分解

## 1. Spec划分与测试点分解的区别

| 对比项 | Spec划分 | 测试点分解 |
|---|---|---|
| 核心目的 | 描述设计需要支持什么功能、接口、约束和性能指标 | 描述如何证明这些功能、接口、约束和性能指标被正确实现 |
| 关注对象 | 设计需求 | 验证目标 |
| 粒度 | 通常按功能章节划分，如协议、位宽转换、复位、APB、性能等 | 通常按可验证场景划分，如边界值、反压、乱序、异常、并发、复位、性能等 |
| 关系 | 一个Spec可能对应多个测试点 | 一个测试点可能覆盖多个Spec |
| 示例 | 上游Burst Length支持0-7，下游支持0-15 | 测LEN=0、LEN=7、LEN转换、WLAST/RLAST位置、反压下Burst转换是否正确 |
| 输出形式 | 规格编号和功能描述 | 测试点、验证内容、检查重点、对应case、覆盖率目标 |

---

## 2. 总体测试点规划表

| 测试类别 | 覆盖规格 | 主要测试目标 | 代表Test Case |
|---|---|---|---|
| 基础读写 | FS002、FS004 | 验证基础AXI读写事务、128/64 bit位宽转换、响应返回 | `tc_sanity`, `tc_rw_common` |
| 握手反压 | FS003、FS006 | 验证AW/W/AR/R/B各通道Valid-Ready delay下数据保持和传输正确性 | `tc_avalid_aready_delay`, `tc_wvalid_wready_delay`, `tc_bvalid_bready_delay`, `tc_rvalid_rready_delay` |
| AW/W耦合 | FS003.001 | 验证AW先到、W先到、AW/W同拍到时，UIF首拍耦合正确 | `tc_avalid_wvalid_delay`, `tc_nextwvalid_delay` |
| Outstanding | FS002.011 | 验证读写多笔Outstanding、最大16笔边界和超过上限反压 | `tc_outstanding` |
| 乱序响应 | FS002.006 | 验证不同ID读写响应乱序返回时，响应ID匹配正确 | `tc_outoforder` |
| 读数据交织 | FS002.005、FS004.005 | 验证不同RID读数据交织返回下的半拍缓存、查找表匹配和拼接 | `tc_interleaving` |
| 错误响应 | FS004.003、FS007 | 验证SLVERR/DECERR响应传播、错误记录、中断置位和清除 | `tc_errresp` |
| APB寄存器 | FS007、FS008 | 验证APB寄存器读写、W1C、PSTRB、PSLVERR、FIFO状态回读 | `tc_apb` |
| 复位 | FS001.002 | 验证双边复位、复位后状态清空、传输中复位恢复 | `tc_rstdir` |
| 时钟频率关系 | FS001.001、FS001.003 | 验证上下游异步时钟、上快下慢、下快上慢、非整数频率比 | `tc_valley_slave`, `tc_valley_master` |
| 性能 | FS006 | 验证CDC和位宽转换不引入额外气泡，低频侧/低位宽侧连续传输 | `tc_perf` |
| 功耗门控 | FS005 | 验证上游自动门控、常开配置、唤醒、测试旁路和下游不门控 | `tc_power` |

---

## 3. 时钟复位测试点

| 测试点 | 覆盖规格 | 验证内容 | 检查重点 |
|---|---|---|---|
| 双异步时钟基础传输 | FS001.001 | 上游和下游使用两个异步时钟，基础读写事务正常完成 | 无丢数、无重复、响应正确 |
| 上游快于下游 | FS001.003 | `s_clk > m_clk`场景下持续发起读写传输 | FIFO反压正确，无死锁 |
| 下游快于上游 | FS001.003 | `m_clk > s_clk`场景下持续读写传输 | 读写通路不引入异常气泡 |
| 非整数频率比 | FS001.003 | 上下游时钟频率无固定比例关系 | CDC FIFO指针同步正确 |
| 双边同时复位 | FS001.002 | 上下游复位同时有效 | FIFO、状态机、Outstanding表清空 |
| 复位释放后恢复传输 | FS001.002 | 复位释放后重新发起读写事务 | valid/ready状态恢复正常 |
| 传输中复位 | FS001.002 | 读写事务进行中拉复位 | 内部状态清空，不产生悬挂事务 |
| 单边复位非法场景约束 | FS001.002 | 规格不允许异步桥单边复位 | 通过环境约束或assertion避免非法单边复位 |

---

## 4. AXI/ACE-Lite协议测试点

| 测试点 | 覆盖规格 | 验证内容 | 检查重点 |
|---|---|---|---|
| AXI4基础写事务 | FS002.001、FS002.002 | AW/W/B完整写流程 | 写数据拆分、B响应正确 |
| AXI4基础读事务 | FS002.001、FS002.002 | AR/R完整读流程 | 读数据拼接、R响应正确 |
| 读写并发 | FS002.006 | 读通道和写通道同时工作 | 读写互不阻塞 |
| AW通道反压 | FS003.002 | AWVALID/AWREADY随机delay | 地址和控制信息保持稳定 |
| W通道反压 | FS003.002 | WVALID/WREADY随机delay | WDATA/WSTRB/WLAST保持稳定 |
| AR通道反压 | FS003.002 | ARVALID/ARREADY随机delay | 读命令保持稳定 |
| R通道反压 | FS003.002 | RVALID/RREADY随机delay | RDATA/RID/RRESP/RLAST保持稳定 |
| B通道反压 | FS003.002 | BVALID/BREADY随机delay | BRESP/BID保持稳定 |
| sideband透传 | FS002.012 | Qos、User、Region、Domain、Cache、Barrier等随路透传 | 上下游字段一致 |
| 不支持AXI3 | FS002.001 | 验证环境只生成AXI4/ACE-Lite合法事务 | 不产生AXI3写交织/WID行为 |
| 不支持写数据间插 | FS002.003 | W通道按AW顺序连续发送 | 不产生交织W数据 |
| 不支持同ID读/写Outstanding | FS002.004 | 同ID读或同ID写不同时Outstanding | 通过VIP约束或assertion保证 |
| 只支持INCR | FS002.007 | 仅生成INCR burst | 不生成FIXED/WRAP |
| 不支持非对齐地址 | FS002.009 | 上游地址16Byte对齐 | 地址低4bit为0 |
| 不支持Narrow | FS002.010 | 上游AxSIZE固定为4 | 不产生窄传输 |

---

## 5. Outstanding、乱序响应与读数据交织测试点

| 测试点 | 覆盖规格 | 验证内容 | 检查重点 |
|---|---|---|---|
| 单笔Outstanding | FS002.011 | 读写各1笔在途事务 | 基础事务正确完成 |
| 多笔Outstanding | FS002.011 | 多个不同ID事务同时在途 | ID跟踪、响应匹配正确 |
| 最大Outstanding | FS002.011 | 读写各16笔达到上限 | 表项、FIFO、计数器边界正确 |
| 超过Outstanding上限 | FS002.011 | 尝试发起第17笔事务 | DUT通过ready反压限制继续接收 |
| 不同ID读响应乱序 | FS002.006 | 后发读命令先返回 | RID匹配正确 |
| 不同ID写响应乱序 | FS002.006 | 后发写命令先返回B响应 | BID匹配正确 |
| 读数据交织 | FS002.005 | R通道出现不同RID穿插返回 | ID查找表、半拍缓存、拼接正确 |
| 同ID顺序约束 | FS002.004 | 同ID读/写不允许多个Outstanding | 环境约束或assertion保证 |

---

## 6. UIF内部协议测试点

| 测试点 | 覆盖规格 | 验证内容 | 检查重点 |
|---|---|---|---|
| AW先到 | FS003.001 | AW先进入FIFO，W后到 | UIF必须等首拍W到达后再发送AWW |
| W先到 | FS003.001 | W先进入FIFO，AW后到 | UIF必须等AW到达后再发送AWW |
| AW/W同拍到 | FS003.001 | AW和W同周期握手 | UIF首拍耦合正确 |
| AWW首拍耦合 | FS003.001 | 写命令和首拍写数据同周期发送 | UIF AWW包原子化传输 |
| Burst后续W连续转发 | FS003.001 | 首拍后只转发后续W数据 | 不重新采样AW，不错误切换事务 |
| UIF反压保持 | FS003.002 | `uaww_valid=1`且`uaww_ready=0` | UIF payload保持稳定 |
| UIF无丢拍 | FS003.002 | AWW/AR/R/B通道valid-ready传输 | 无丢数、无重复、无乱序错误 |

---

## 7. 位宽转换测试点

### 7.1 命令转换测试点

| 测试点 | 覆盖规格 | 验证内容 | 检查重点 |
|---|---|---|---|
| SIZE转换 | FS004.001 | 上游`SIZE=4`转换为下游`SIZE=3` | SIZE字段转换正确 |
| LEN转换 | FS004.002、FS002.008 | 上游`LEN=N`转换为下游`LEN=2N+1` | 总数据量一致 |
| 命令不拆分 | FS004.002 | 上游一条命令对应下游一条命令 | 不产生多条下游命令 |
| LEN最小值 | FS002.008 | 上游`LEN=0` | 下游`LEN=1` |
| LEN最大值 | FS002.008 | 上游`LEN=7` | 下游`LEN=15` |
| 地址保持 | FS004.001 | INCR命令起始地址透传 | 起始地址不变 |
| 总数据量一致 | FS004.002 | 转换前后传输字节数一致 | `(LEN+1) * bytes_per_beat`一致 |

### 7.2 写数据拆分测试点

| 测试点 | 覆盖规格 | 验证内容 | 检查重点 |
|---|---|---|---|
| 低64 bit先发 | FS004.005 | 128 bit WDATA拆成两拍64 bit | 第一拍为低64 bit |
| 高64 bit后发 | FS004.005 | 第二拍发送高64 bit | 数据顺序正确 |
| WSTRB拆分 | FS004.005 | 16 bit WSTRB拆成两个8 bit | strobe与数据对应 |
| WLAST转换 | FS004.005 | 上游最后一拍对应下游最后一个64 bit拍 | 下游最后一拍WLAST=1 |
| Burst连续拆分 | FS004.005 | 多拍128 bit Burst连续拆成两倍64 bit拍 | 数据顺序连续正确 |
| W通道反压下拆分 | FS003.002、FS004.005 | 下游WREADY delay | 数据、WSTRB、WLAST保持正确 |

### 7.3 读数据拼接测试点

| 测试点 | 覆盖规格 | 验证内容 | 检查重点 |
|---|---|---|---|
| 先收到低64 | FS004.005 | 第一拍64 bit放入128 bit低64位置 | 拼接低半部分正确 |
| 后收到高64 | FS004.005 | 第二拍64 bit放入128 bit高64位置 | 拼接高半部分正确 |
| RLAST转换 | FS004.005 | 下游最后一个64 bit响应合并后返回上游 | 上游对应128 bit响应RLAST正确 |
| RRESP合并 | FS004.003 | 两拍中任意一拍出错 | 返回第一个Error响应 |
| RUSER合并 | FS004.004 | 两拍RUSER合并 | 取下游第一拍User |
| 多ID交织拼接 | FS002.005、FS004.005 | 不同RID交织返回 | 查找表缓存半拍并按ID拼接 |

---

## 8. 错误响应与可维可测测试点

| 测试点 | 覆盖规格 | 验证内容 | 检查重点 |
|---|---|---|---|
| OKAY响应 | FS004.003 | 下游返回OKAY | 不触发错误中断 |
| SLVERR响应 | FS004.003、FS007.001 | 下游返回SLVERR | 上游响应正确，中断置位 |
| DECERR响应 | FS004.003、FS007.001 | 下游返回DECERR | 上游响应正确，中断置位 |
| 不支持EXOKAY | FS004.003 | 下游不应返回EXOKAY | 环境约束或assertion检查 |
| 读错误记录 | FS007.003 | 读响应错误时记录ADDR/ID | 读错误寄存器记录正确 |
| 写错误记录 | FS007.003 | 写响应错误时记录ADDR/ID | 写错误寄存器记录正确 |
| 读写错误独立记录 | FS007.003 | 读错误和写错误同时或先后发生 | 读写错误信息互不覆盖 |
| 第一笔错误锁存 | FS007.003 | 多笔错误连续发生 | 只记录第一笔错误 |
| 中断mask | FS007.002 | 配置中断屏蔽 | 中断输出被屏蔽 |
| 中断写1清除 | FS007.002 | APB写1清除中断 | 中断状态清除 |
| clear后再次记录 | FS007.003 | 清除后再次发生错误 | 可重新记录新错误 |
| FIFO状态回读 | FS007.004 | APB读取内部FIFO空满状态 | 状态寄存器正确 |
| 握手状态回读 | FS007.004 | APB读取各Valid-Ready接口状态 | 握手状态可观测 |

---

## 9. APB4配置接口测试点

| 测试点 | 覆盖规格 | 验证内容 | 检查重点 |
|---|---|---|---|
| APB基础写 | FS008.001、FS008.002 | APB写寄存器 | PSEL/PENABLE/PWRITE/PADDR/PWDATA时序正确 |
| APB基础读 | FS008.001、FS008.002 | APB读寄存器 | PRDATA返回正确 |
| 地址位宽 | FS008.002 | 12 bit地址空间访问 | 地址译码正确 |
| 数据位宽 | FS008.002 | 32 bit寄存器读写 | 数据位宽正确 |
| 不区分安全/非安全 | FS008.003 | 安全属性不影响寄存器访问 | 安全/非安全访问行为一致 |
| PSTRB全1写有效 | FS008.004 | `pstrb=4'b1111` | 写操作生效 |
| PSTRB非全1写无效 | FS008.004 | `pstrb!=4'b1111` | 寄存器不被修改 |
| PSLVERR检查 | FS008.005 | 不支持返回PSLVERR | PSLVERR保持无错误 |
| W1C寄存器 | FS007.002 | 写1清除寄存器 | 写1清除，写0保持 |
| RO寄存器 | FS007.004 | 只读状态寄存器 | 软件写无效 |
| 默认值检查 | FS008.001 | 复位后读取寄存器 | 默认值正确 |

---

## 10. 自动时钟门控测试点

| 测试点 | 覆盖规格 | 验证内容 | 检查重点 |
|---|---|---|---|
| 自动门控开启 | FS005.001 | 无操作时上游时钟自动关闭 | idle后关钟 |
| 有上游操作时开钟 | FS005.001 | AW/AR/W/APB访问到来 | 上游时钟打开 |
| 操作未完成保持开钟 | FS005.001 | Outstanding未清零或FIFO非空 | 不能误关钟 |
| 响应未返回保持开钟 | FS005.001 | B/R响应未完成 | 不能提前关钟 |
| 读拼接未完成保持开钟 | FS005.001 | 读数据拼接表中存在半拍数据 | 不能关钟 |
| idle后自动关钟 | FS005.001 | 所有事务完成后进入idle | 时钟关闭 |
| 寄存器关闭门控 | FS005.002 | 配置关闭自动门控 | 上游时钟常开 |
| 门控旁路 | FS005.002 | test/scan或配置旁路 | 门控不生效 |
| 下游不门控 | FS005.003 | 下游时钟持续运行 | 下游时钟不受影响 |
| 门控恢复传输 | FS005.001 | 关钟后新请求唤醒 | 事务正常完成 |

---

## 11. 性能与无气泡传输测试点

| 测试点 | 覆盖规格 | 验证内容 | 检查重点 |
|---|---|---|---|
| CDC无额外气泡 | FS006.001 | 高频向低频传输，且高频侧带宽充足 | 低频侧连续valid-ready握手 |
| 位宽转换无额外气泡 | FS006.002 | 128 bit向64 bit转换 | 64 bit侧连续输出 |
| 写通道满带宽 | FS006.001、FS006.002 | 连续写Burst | 下游W通道无内部插空 |
| 读通道满带宽 | FS006.001、FS006.002 | 连续读返回 | 上游R通道无内部插空 |
| show-ahead FIFO性能 | FS006.001 | FIFO非空且下游ready | 读侧不额外等待一拍 |
| skid buffer反压恢复 | FS006.001 | 短暂反压后解除 | 快速恢复连续传输 |
| 不同频率比性能 | FS001.003、FS006.001 | 上快下慢、下快上慢、非整数频率比 | 无死锁、无异常气泡 |
| 最大Outstanding性能 | FS002.011、FS006 | 16笔Outstanding压力场景 | 吞吐达到预期 |

---

## 12. 回归测试映射表

| Test Case | 主要覆盖测试点 |
|---|---|
| `tc_sanity` | 基础冒烟测试，覆盖单笔读写、复位后基本传输 |
| `tc_rw_common` | 常规读写、位宽转换、B/R响应返回 |
| `tc_avalid_aready_delay` | AW/AR地址通道Valid-Ready反压 |
| `tc_wvalid_wready_delay` | W写数据通道Valid-Ready反压 |
| `tc_wbvalid_arvalid_delay` | 写响应与读地址交叠、错峰、并发场景 |
| `tc_nextrvalid_delay` | 连续R返回数据之间插入delay |
| `tc_avalid_wvalid_delay` | AW先到、W先到、AW/W同拍到 |
| `tc_nextwvalid_delay` | 写Burst内部W拍之间插入delay |
| `tc_bvalid_bready_delay` | B响应通道反压 |
| `tc_rvalid_rready_delay` | R读数据通道反压 |
| `tc_nextavalid_delay` | 连续地址命令之间插入delay |
| `tc_outstanding` | 多Outstanding、最大16笔边界、超过上限反压 |
| `tc_outoforder` | 不同ID读写响应乱序返回 |
| `tc_interleaving` | 不同ID读数据交织、半拍缓存与拼接 |
| `tc_aready_wready_delay` | 地址通道和写数据通道同时/分别反压 |
| `tc_bready_rready_delay` | B/R响应通道同时/分别反压 |
| `tc_errresp` | SLVERR/DECERR、错误记录、中断置位与清除 |
| `tc_apb` | APB寄存器读写、PSTRB、W1C、状态回读 |
| `tc_rstdir` | 复位方向、复位释放、传输中复位 |
| `tc_valley_slave` | 下游低吞吐、周期性反压场景 |
| `tc_valley_master` | 上游低吞吐、请求/接收不连续场景 |
| `tc_perf` | 满带宽、无气泡传输、吞吐性能 |
| `tc_power` | 自动门控、常开配置、唤醒、门控旁路 |
