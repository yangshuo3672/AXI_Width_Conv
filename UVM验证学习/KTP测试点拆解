hvp plan P530	"skip
feature number"	feature	"skip
testpoint number"	feature	"skip
fake coverage code"	"skip
Testcase number"	"skip
Doc Section"	$weight	value snps.Group	value snps.Assert	value snps.test	goal.test	measure snps.source
plan		设计规格		测试点									
时钟复位													
	FS001.001	上游和下游各一个输入时钟，两个时钟为异步关系，收敛频率均不低于1GHz@0.8v	TP.FS001.001.001	上下游异步时钟									
	FS001.002	上游和下游各一个输入复位，异步复位，同步撤离到对应时钟域（不支持单边复位）	TP.FS001.002.001	上下游复位同时有效									
			TP.FS001.002.002	复位释放后重新发起读写事务且无丢失数据									
	FS001.003	上下游的时钟频率之间无约束关系，支持上游时钟频率大于或小于下游的场景；	TP.FS001.003.001	上游大于下游时钟									
			TP.FS001.003.002	上游小于下游时钟									
协议/接口													
	FS002.001	支持ACE-Lite，AXI4协议，不支持AXI3协议	TP.FS002.001.001	上下游AW通道AWVALID/AWREADY反压									
			TP.FS002.001.002	上下游AR通道ARVALID/ARREADY反压									
			TP.FS002.001.003	上下游W通道WVALID/WREADY反压									
			TP.FS002.001.004	上下游B通道BVALID/BREADY反压									
			TP.FS002.001.005	上下游R通道RVALID/RREADY反压									
			TP.FS002.001.006	可以处理上游写命令早于写数据到达									
			TP.FS002.001.007	可以处理上游写数据早于写命令到达									
			TP.FS002.001.008	可以处理上游写数据和写命令同时到达									
													
	FS002.002	地址位宽32bit，上游数据位宽128bit，下游数据位宽64bit	TP.FS002.002.001	W通道写数据拆分正确，B通道写响应返回正确									
			TP.FS002.002.002	R通道读数据拼接正确，读响应返回正确									
			TP.FS002.002.003	读写命令中，下游发送的（Burst Length+1）是上游（Burst Length+1）的两倍									
	FS002.005	支持不同ID读命令的读数据间插	TP.FS002.005.001	支持不同ID的读数据检查返回									
	FS002.006	支持不同ID的读写响应乱序返回	TP.FS002.006.001	支持读响应乱序返回，即支持后发的命令先返回响应									
			TP.FS002.006.002	支持写响应乱序返回									
	FS002.007	只支持INCR传输	TP.FS002.007.001	支持INCR类型的突发传输									
	FS002.008	上游Burst Length支持0~7，下游Burst Length支持0~15，不支持超长Burst	TP.FS002.008.001	上游支持最大突发长度为8									
			TP.FS002.008.002	下游支持最大突发长度为16									
	FS002.0011	支持的读写最大Outstanding为各16，即同时最多只能接收16个上游下发的读命令和16个上游下发的写命令	TP.FS002.0011.001	写响应返回之前，最多支持16个未完成的写操作									
			TP.FS002.0011.002	读响应返回之前，最多支持16个未完成的读操作									
	FS002.0011	支持协议中的Qos、User、Region、Domain、Cache、Barrier等随路信号透传	TP.FS002.0012.002	支持协议中的Qos、User、Region、Domain、Cache、Barrier等随路信号透传，不丢失									
位宽转换													
	FS004.001	对于INCR命令，将命令转换为输出端口位宽（即SIZE=3）的INCR发送	TP.FS004.001.001	上游命令Size为4，下游命令Size为3									
	FS004.003	读响应Merge时，任何一个响应有Error，则向上返回第一个有Error的响应	TP.FS004.003.001	读响应Merge时，两拍中返回第一拍有Error的响应									
	FS004.004	向上游返回Buser和Ruser时，Merge完的User以下游返回的两拍中的第一拍为准	TP.FS004.004.001	读响应Merge时，上游返回下游两拍中第一排的User									
	FS004.005	对于数据的转换，Wdata从128bit转换为2拍64bit时，先下发128bit数据中的低64bit；Rdata从2拍64bit转换为128bit时，先收到的64bit数据放在128bit的低64bit位置	TP.FS004.005.001	写数据先下发128bit数据中的低64bit									
			TP.FS004.005.002	读数据先返回的64bit数据放在128bit的低64bit位置									
自动门控													
													
													
性能													
	FS006.001	跨异步传输不引入额外性能损失，即高频向低频传输，且高频时钟域带宽>=低频时钟域带宽时，低频时钟域数据传输无气泡	TP.FS006.001.001	上游时钟大于下游时钟时，下游写数据传输无气泡									
			TP.FS006.001.002	下游时钟大于上游时钟时，上游读数据返回传输无气泡									
	FS006.002	位宽转换不引入额外性能损失，即高位宽向低位宽传输，且高位宽侧带宽>=低位宽侧带宽时，低位宽侧数据传输无气泡	TP.FS006.002.001	上游传输带宽大于下游传输带宽时，下游写命令通道无气泡									
			TP.FS006.002.002	上游传输带宽大于下游传输带宽时，下游写数据通道无气泡									
			TP.FS006.002.003	上游传输带宽大于下游传输带宽时，下游读数据通道无气泡									
可维可测													
	FS007.001	支持检测到下游返回ERROR响应时上报电平中断	TP.FS007.001.001	检测到B通道返回写响应出现ERROR时上报电平中断		XXX_XXX.sv							
			TP.FS007.001.002	检测到R通道返回写响应出现ERROR时上报电平中断									
	FS007.002	中断可以通过寄存器配置屏蔽，可以通过寄存器写1清除	TP.FS007.002.001	可通过APB配置接口对特定地址写1清除中断		XXX_XXX.sv							
	FS007.003	支持记录发生错误的命令及其相关信息（ADDR/ID），并通过寄存器回读；发生错误时只记录第一笔出错命令的地址和ID	TP.FS007.003.001	支持记录写响应发生错误时的ID和ADDR，支持寄存器回读		XXX_XXX.sv							
			TP.FS007.003.002	支持记录读响应发生错误时的ID和ADDR，支持寄存器回读									
			TP.FS007.003.003	响应发生错误时只记录第一笔错误ID和ADDR									
	FS007.004	支持通过寄存器回读内部FIFO的空满状态及各Valid-Ready接口的握手状态；	TP.FS007.004.001	支持寄存器回读内部fifo状态与各接口握手状态									
配置接口/寄存器													
	FS008.001	支持APB4.0接口访问内部寄存器	TP.FS008.001.001	APB基础写，RW寄存器写入后读出正确，W1C寄存器写1可以使能		XXX_XXX.sv							
			TP.FS008.001.002	APB基础读，RW和RO寄存器读出数据正确									
			TP.FS008.001.003	RO寄存器写入后无效，值不会改变									
			TP.FS008.001.004	复位后默认值读出正确									
			TP.FS008.001.005	读取非法地址空间时为0									
	FS008.002	地址位宽12bit，数据位宽32bit	TP.FS008.002.001	地址译码正确，能够按照读取地址									
													
													
													
													
